<%= form_with(model: username) do |form| %>
  <% if username.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(username.errors.count, "error") %> prohibited this username from being saved:</h2>

      <ul>
        <% username.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
